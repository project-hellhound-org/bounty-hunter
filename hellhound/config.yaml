profiles:
  quick:
    - nmap

  web:
    - nmap
    - vhost

  full:
    - nmap
    - vhost

default_wordlist: "wordlists/default.txt"

modules:
  nmap:
    description: Network reconnaissance and service detection

  reconcombo:
    description: Full web reconnaissance pipeline (subdomains, URLs, JS, GF, nuclei)

  dirsearch:
    description: Directory and endpoint discovery for web targets

  vhost:
    description: Virtual host enumeration using ffuf

  ftp:
    description: FTP enumeration and anonymous login checks
